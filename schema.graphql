type UserHistory {
  id: ID!
  user: String! # (address)
  operation: String! # Delegated, DelegateAmountIncreased, DelegateUnstakingInitiated, DelegateUnstaked, DelegateLockupIncreased
  amount: BigInt!
  period: BigInt!
  blockTimestamp: Int!
  transactionHash: String!
}
